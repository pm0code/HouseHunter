# Session Handoff — 2026-05-23

## Resume Command
```
cd D:/projects/HouseHunter && claude
```

## State at Handoff

**Branch**: `main` — commit `3effd13` pushed to `https://github.com/pm0code/HouseHunter`

**Dev server**: Not running (start with `npm run dev` — port 3100)

**Docker services**: Verify with `docker compose ps`
- postgres:5433, valkey:6380, osrm:5000, nominatim:7070, mailpit:8025

---

## What Was Completed This Session

### 8 PRD v2.1 Gaps — All Fixed

| Gap | PRD Ref | File | Status |
|-----|---------|------|--------|
| G1 Route line on map | FR-2.5 | `MapView.tsx` | Done — amber dashed walk line from listing to subway |
| G2 Date filter WHERE clause | FR-1.2 | `api/listings/route.ts` | Done — `availableFrom`/`durationDays` now in DB query |
| G3 Photo carousel | FR-3.1 | `PropertyDetailPanel.tsx` | Done — prev/next/dots/counter |
| G4 Marker clustering | FR-2.2 | `MapView.tsx` | Done — native MapLibre clustering, click-to-expand |
| G5 WCAG color-only safety | NFR-7 | `MapView.tsx` + `MapLayout.tsx` | Done — legend + tier text labels at zoom>=13 |
| G6 Safety legend | FR-2.4 | `MapLayout.tsx` | Done — SafetyLegend component, bottom-right overlay |
| G7 Price default mismatch | FR-1.3 | `types/index.ts` | Done — `maxPrice` 3500→5000 |
| G8 Escape key handler | NFR-7 WCAG | `PropertyDetailPanel.tsx` | Done — closes panel on Escape |

### Supporting Fixes
- `getClusterExpansionZoom` callback→Promise API (MapLibre GL JS v5 breaking change)
- TypeScript: 0 errors confirmed before commit

### Infrastructure Created
- `README.md` — full project documentation with setup, architecture, env vars, troubleshooting
- `HouseHunter.ps1` — one-command setup + launch script (prereqs check, Docker, DB migration, dev server)

---

## What Remains (Prioritized)

### High — MVP Completeness

**Mobile layout (FR-4.1, FR-4.2)** — NOT STARTED
- Bottom-sheet drawer on mobile viewports (<=768px)
- Filter modal triggered from bottom nav
- Modify: `MapLayout.tsx` (responsive breakpoints), new `BottomSheet.tsx` component
- Approach: Tailwind responsive classes + drawer using `useSearchStore` for open/close state

**Listing click → Results tab auto-switch** — UX gap (obvious, easy fix)
- When user clicks a map pin, `activeTab` stays on 'search'
- Fix: call `setActiveTab('results')` inside `m.on('click', 'listings-unclustered')` in `MapView.tsx` ~line 214

### Low — Out of MVP Scope (v2.1)
- Isochrone overlay (v2.1): travel-time rings — requires OSRM isochrone or Valhalla
- Hex-bin heatmap (v2.1): density visualization — requires turf.js or H3
- LeaseBreak / RentHop scrapers: both return 403 currently

---

## Key Architecture Facts

- **Port map (Constitution)**: Next.js 3100 · PostgreSQL 5433 · Valkey 6380 · OSRM 5000 · Nominatim 7070 · Mailpit 8025
- **FOSS-only**: Every dep must have OSI-approved license — no Mapbox, no Vercel, no commercial SaaS
- **Tiles**: PMTiles at `/public/tiles/new-york-city.pmtiles` — served as static file by Next.js
- **DB**: Drizzle ORM → PostgreSQL 17 + PostGIS 3.5. Schema in `src/lib/db/schema.ts`
- **Cache**: Valkey — listing queries cached, TTL in `src/lib/valkey.ts`
- **State**: nuqs (URL params) · Zustand (map/UI) · TanStack Query (server data)
- **Scrapers**: `infra/scrapers/` — Craigslist, SpareRoom, CozyCozy working; 541 real listings in DB

## Files Changed This Session
```
src/app/api/listings/route.ts
src/components/listings/PropertyDetailPanel.tsx
src/components/map/MapLayout.tsx
src/components/map/MapView.tsx
src/types/index.ts
README.md          (new)
HouseHunter.ps1    (new)
```
